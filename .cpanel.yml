---
deployment:
  tasks:
    - export DEPLOYPATH=/data0/downloadlounge.com/public_html
    - /bin/mkdir -p $DEPLOYPATH/downloadloungecom && /bin/cp -rf downloadlounge.com/. $DEPLOYPATH/downloadloungecom/
